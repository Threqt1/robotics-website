const welcomes = [
    "Welcome back retard",
    "Good morning bitch boy",
    "Good evening dickface",
    "Back for more, dumbass?"
]

export const random = welcomes[Math.floor(Math.random() * welcomes.length)];